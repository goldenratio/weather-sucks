language: node_js
node_js: 7

before_script:
- npm install
- npm install -g firebase-tools

jobs:
  include:
    - stage: Typecheck
      script: npm run typecheck
    - stage: Deploy to Firebase
      script: npm run deploy